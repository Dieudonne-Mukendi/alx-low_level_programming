#include "holberton.h"
/**
 * add - adds two integers and returns the result
 *
 * @n: is the first number
 * @m: is the second number
 * Return: the sum value of @n and @m
 */
int add(int n, int m)
{
	return (n + m);
}
